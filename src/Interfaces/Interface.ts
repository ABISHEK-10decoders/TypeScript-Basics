export interface PropsApp {
    textarea : string;
    number ? : number;
}

export interface Users {
    
        "userId": number;
        "id": number;
        "title": string;
        "body": string;
      
}

export interface InputState {
    title: string;
    body: string;
}
